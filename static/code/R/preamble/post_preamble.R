

knitr::opts_chunk$set(echo = TRUE, root.dir = "../",
                      tidy = TRUE,
                      class.source = "watch-out",
                      class.output = "watch-out",
                      fig.align = "center",
                      fig.topcaption=TRUE
)


options(width = 1000)

library(kableExtra)